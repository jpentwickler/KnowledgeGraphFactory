# Core dependencies
anthropic>=0.40.0
python-dotenv>=1.0.0

# MCP server
fastmcp<3

# Neo4j (for Stage 6)
neo4j>=5.0.0

# Data validation (for Stage 6)
pandas>=2.0.0

# Neo4j GraphRAG for entity extraction + subject/lexical graph (US009)
neo4j-graphrag>=1.0.0

# Fuzzy string matching for entity resolution (US009)
rapidfuzz>=3.0.0

# Required by neo4j-graphrag for LLM + embeddings (US009)
openai>=1.0.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
