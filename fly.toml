app = "kg-query"
primary_region = "ams"

[build]
  dockerfile = "Dockerfile.query"

[http_service]
  internal_port = 8000
  force_https = true

  [[http_service.checks]]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    path = "/health"
    timeout = "5s"
